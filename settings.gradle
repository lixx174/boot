rootProject.name = 'boot'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("libs.versions.toml"))
        }
    }
}

include 'boot-admin'
include 'boot-authorization'

